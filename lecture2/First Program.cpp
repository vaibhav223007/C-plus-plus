// First Program //

#include<iostream> /* we are saying we need to include a file called iostream in our code. Why? Because it has functions such as cout in it */
using namespace std; /* There are many namespace in c++ so here we are saying that we want to use namespace called "std" */

int main() {  /* This is the start of our program, whenever complier need to execute any program it will search for this as it's starting point */
    cout << "Namaste Dunya :-)" << endl; /* cout is a function in iostream file which help us to display standard output. We use << along with cout to display standard output. In cout we are writting what we want to dispay
    . endl is like a enter we use it to send our cursor to new line. ";" is like a full stop to tell compiler that this line ends here. */
}



